/**
 * Domain Adapter Types — Re-exported for convenience.
 *
 * Import from '@sovereign-labs/narrowing/types' for the full type set.
 * Import from here for adapter-specific types only.
 */

export type {
  DomainAdapter,
  SignaturePattern,
  FailureKind,
} from '../types.js';
